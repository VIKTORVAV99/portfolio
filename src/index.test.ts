import { describe, it, expect } from 'bun:test';

// This is a simple test to ensure that the test suite is working
describe('sum test', () => {
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
});
